# -*- mode: python -*-

Import('env')

env = env.Clone()

# xxHash is a header-only library when XXH_INLINE_ALL is defined
# We create a library target for dependency tracking
env.Library(
    target='xxhash',
    source=[
        'xxhash_impl.cpp',
    ],
    LIBDEPS=[],
)
